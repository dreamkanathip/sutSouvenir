import { FormatDateToThaiPipe } from './format-date-to-thai.pipe';

describe('FormatDateToThaiPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDateToThaiPipe();
    expect(pipe).toBeTruthy();
  });
});
