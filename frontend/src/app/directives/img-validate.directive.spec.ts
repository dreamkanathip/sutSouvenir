import { ImgValidateDirective } from './img-validate.directive';

describe('ImgValidateDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgValidateDirective();
    expect(directive).toBeTruthy();
  });
});
